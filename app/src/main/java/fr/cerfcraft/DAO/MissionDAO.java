package fr.cerfcraft.DAO;

import java.util.HashMap;
import java.util.Map;

import fr.cerfcraft.model.Mission;

public class MissionDAO {

/*    public Mission HashToMissionObject(Map<String, Object> map){
        Mission mission = new Mission();
        for (HashMap.Entry<String, Object> entry : map.entrySet())
        {
            switch(entry.getKey()){
                case "name":
                    mission.setName(entry.getValue().toString());
                    break;
                case "image":
                    mission.setImage(entry.getValue().toString());
                    break;
                case "description":
                    mission.setDescription(entry.getValue().toString());
                    break;
            }
        }
        mission.missionList.add(mission);
        return mission;
    }*/
}
